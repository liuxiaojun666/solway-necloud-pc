ajaxData({}, {})('monitoringSummaryCtrl', ['$scope', 'myAjaxData'], ($scope, myAjaxData) => {
    // 日期更新后回调   range 时间范围（日月年累计）    date 为日期
    $scope.dateUpdated = (range, date) => {
        
    };
});