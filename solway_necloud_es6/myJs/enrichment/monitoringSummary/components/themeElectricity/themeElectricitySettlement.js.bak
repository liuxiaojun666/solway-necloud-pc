app.directive('themeElectricitySettlement', ['$timeout', ($timeout) => ({
    restrict: 'E',
    replace: !0,
    templateUrl: 'tpl/enrichment/monitoringSummary/components/themeElectricity/themeElectricitySettlement.html',
    scope: {
        resData: '='
    },
    link($scope, $element, $attrs) {
        $timeout(() => {
            $element.css('display', 'block');
        }, 0)
    },
})])