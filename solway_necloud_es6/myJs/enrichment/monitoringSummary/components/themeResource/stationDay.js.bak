app.directive('resourceStationDay', ['myAjaxData', '$ocLazyLoad', '$timeout', (myAjaxData, $ocLazyLoad, $timeout) => ({
    restrict: 'E',
    replace: !0,
    templateUrl: 'tpl/enrichment/monitoringSummary/components/themeResource/stationDay.html',
    scope: {
    },
    link($scope, $element, $attrs) {
        $timeout(() => {
            $element.css('display', 'block');
        }, 0)
    },
    controller() {
        
    }
})])