app.directive('radiateCalendar', ['myAjaxData', '$ocLazyLoad', (myAjaxData, $ocLazyLoad) => ({
    restrict: 'E',
    replace: !0,
    templateUrl: 'tpl/enrichment/monitoringSummary/components/themeResource/radiateCalendar.html',
    scope: {
    },
    link($scope, $element, $attrs) {

    },
    controller($scope) {
        $scope.calendarList = new Array(30).fill('').map((v, i) => ({
            day: 1528560000000 + i * 60 * 60 * 24 * 1000,
            grossRadiationIntensity: ["2.7807", "kWh/㎡"],
            scoreRadiationIntensity: 1.5,
            weaterTag: "B"
        }));
    }
})])