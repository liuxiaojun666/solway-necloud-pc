ajaxData({}, {})('dataShareSummaryCtrl', ['$scope', 'myAjaxData'], ($scope, myAjaxData) => {
    $(".align-middle").css('paddingTop', ($(window).height() - 200) / 2)
    $scope.moduleName = '数据共享';//当前页面名称；
});