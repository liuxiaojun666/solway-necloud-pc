ajaxData({
   
}, {
        __serviceName__: 'peopleInfoTplService'
    })('peopleInfoCtrl', ['$scope', 'peopleInfoTplService'], ($scope, myAjaxData) => {

      

        $scope.$on('$destroy', () => {
            // window.removeEventListener('resize', resizeHeight);
        });

    });