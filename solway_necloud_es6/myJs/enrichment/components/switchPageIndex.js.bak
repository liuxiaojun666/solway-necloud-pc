app.directive('switchIndexPage', ['$ocLazyLoad', '$http', '$state', '$timeout', ($ocLazyLoad, $http, $state, $timeout) => ({
    restrict: 'E',
    transclude: !0,
    replace: !0,
    scope: {

    },
    templateUrl: 'tpl/enrichment/components/switchIndexPage.html',
    link($scope, $element) {

        $scope.curentPage = 'pageEarth';
        $scope.switchPageTab = function(page){
            $scope.curentPage = page;
        }
        
    }
})])
