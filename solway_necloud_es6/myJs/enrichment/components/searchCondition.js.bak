app.directive('searchCondition', ['myAjaxData', '$ocLazyLoad', (myAjaxData, $ocLazyLoad) => ({
    restrict: 'E',
    replace: !0,
    templateUrl: 'tpl/enrichment/components/searchConditionTpl.html',
    scope: {
        tabNames: "=",
        changeTab: '='
    },
    link($scope, $element) {

        $scope.showSearchModel = () =>{
            
        }

        $scope.$on('$destroy',function(){
        })
    

        
    }
})])