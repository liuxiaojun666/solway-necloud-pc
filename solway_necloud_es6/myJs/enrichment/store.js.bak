((function () {
    const userInfo = {};
    const stationInfo = {};
    const alarm = {};
    const systemInfo = {};
    const actionRecord = {};

    $solway.persist({
        userInfo,
        stationInfo,
        alarm,
        systemInfo,
        actionRecord,
        stamp: {
            stamp: window.name || window.localStorage.stamp
        },
    });

    // $solway.setStore('stamp', { stamp: window.name || window.localStorage.stamp});
    window.stamp = window.name = window.name || $solway.getStore().stamp.stamp;

    // $solway.getStore();   获取
    // $solway.setStore('userInfo', {});   设置
})());