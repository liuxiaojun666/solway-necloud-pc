((function () {
    const userInfo = {};
    const stationInfo = {};
    const alarm = {};

    $solway.persist({
        userInfo,
        stationInfo,
        alarm
    });

    // $solway.getStore();   获取
    // $solway.setStore(name, {});   设置
})());