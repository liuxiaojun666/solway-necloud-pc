((function () {
    const userInfo = {};
    const stationInfo = {};
    const alarm = {};
    const actionRecord = {};

    $solway.persist({
        userInfo,
        stationInfo,
        alarm,
        actionRecord,
    });

    // $solway.getStore();   获取
    // $solway.setStore('userInfo', {});   设置
})());